# Copyright 2022 Canonical Ltd.
# See LICENSE file for licensing details.
name: metadata-tester
description: tester charm that implements the requirer side of a one-way data exchange relation
summary: tester charm that implements the requirer side of a one-way data exchange relation
type: charm
platforms:
  ubuntu@20.04:amd64:

config:

requires:
  metadata:
    limit: 1
    interface: grafana_metadata

parts:
  charm:
    charm-binary-python-packages:
      - cryptography
      - ops
      - pydantic-core
      - pydantic
      - maturin
